# Model Artifacts

This directory can contain small final model metadata and links to external
artifacts.

For large checkpoints, prefer external storage:

- GitHub Releases
- Hugging Face Hub
- Google Drive
- Course submission storage

Recommended metadata format:

```text
model_name:
  task:
  base_model:
  training_data:
  metrics:
  artifact_url:
```
